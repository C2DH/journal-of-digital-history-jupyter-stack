install.packages(
  c(
    "ggplot2"
  )
)

# here add libraries needed to run the notebook in the following way (replace the above part with the below one, and replace/add libraries you need to use):

# install.packages(
#   c(
#     "ggplot2",
#     "dplyr",
#     "tidyr",
#     "readr"
#   )
# )

